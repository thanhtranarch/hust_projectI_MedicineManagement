# MediManager configuration
#
# This file is OPTIONAL. With no .env at all the application stores its data in
# data/medimanager.db and needs no setup.
#
# Copy it to .env only if you want the database file somewhere else.

SQLITE_PATH=data/medimanager.db
