# Core application
PyQt6>=6.4.0
bcrypt>=4.0.1
darkdetect>=0.8.0
python-dotenv>=1.0.0
reportlab>=4.0.0

# PostgreSQL / Supabase backend.
# Not required when running on the default SQLite backend.
psycopg2-binary>=2.9.9
